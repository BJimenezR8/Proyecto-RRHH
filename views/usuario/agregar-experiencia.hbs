<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{#if id}}Editar Experiencia Laboral{{else}}Agregar Experiencia Laboral{{/if}} - Sistema de Reclutamiento de HR</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <div class="container">
        <header class="header">
            <h1>{{#if id}}Editar Experiencia Laboral{{else}}Agregar Experiencia Laboral{{/if}}</h1>
            <nav class="navigation">
                <ul>
                    <li><a href="/auth/perfil" class="nav-link">Volver al Perfil</a></li>
                    <li><a href="/login" class="nav-link button-secondary">Cerrar Sesión</a></li>
                </ul>
            </nav>
        </header>
        <main class="main-content">
            <section class="dashboard-section">
                <form class="form" action="{{#if id}}/auth/actualizarExperiencia{{else}}/auth/agregarExperiencia{{/if}}" method="POST">
                    {{#if id}}
                        <input type="hidden" name="id" value="{{id}}">
                    {{/if}}
                    <div class="form-group">
                        <label for="empresa">Empresa:</label>
                        <input type="text" id="empresa" name="empresa" value="{{empresa}}" required>
                    </div>
                    
                    <div class="form-group">
                        <label for="puesto_ocupado">Puesto Ocupado:</label>
                        <input type="text" id="puesto_ocupado" name="puesto_ocupado" value="{{puesto_ocupado}}" required>
                    </div>
                    
                    <div class="form-group">
                        <label for="fecha_desde">Fecha Desde:</label>
                        <input type="date" id="fecha_desde" name="fecha_desde" value="{{fecha_desde}}" required>
                    </div>
                    
                    <div class="form-group">
                        <label for="fecha_hasta">Fecha Hasta:</label>
                        <input type="date" id="fecha_hasta" name="fecha_hasta" value="{{fecha_hasta}}" required>
                    </div>
                    
                    <div class="form-group">
                        <label for="salario">Salario:</label>
                        <input type="number" id="salario" name="salario" value="{{salario}}" required>
                    </div>
                    
                    <div class="button-container">
                        <button type="submit" class="button button-primary">{{#if id}}Actualizar{{else}}Agregar{{/if}}</button>
                    </div>
                </form>
            </section>
        </main>
        <footer class="footer">
            <p>&copy; 2023 Sistema de Reclutamiento de HR. Todos los derechos reservados.</p>
        </footer>
    </div>
</body>
</html>