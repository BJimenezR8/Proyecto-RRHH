<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Panel de Empleado - Sistema de Reclutamiento de HR</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <div class="container">
        <header class="header">
            <h1>Panel de Empleado</h1>
            <p class="welcome-message">Bienvenido, <span id="employee-name">{{username}}</span></p>
            <nav class="navigation">
                <ul>
                    <li><a href="#" class="nav-link">Página Principal</a></li>
                    <li><a href="/auth/perfil" class="nav-link">Perfil</a></li>
                    <li><a href="/auth/puestos" class="nav-link">Aplicar a Posiciones</a></li>
                    <li><a href="/login" class="nav-link button-secondary">Cerrar Sesión</a></li>
                </ul>
            </nav>
        </header>
        <main class="main-content">
            <section id="aplicaciones" class="dashboard-section">
                <h2>Mis Aplicaciones</h2>
                <div id="crud-aplicaciones" class="crud-container">
                    <!-- Aquí iría el CRUD de las aplicaciones -->
                    <table class="crud-table">
                <thead>
                    <tr>
                        <th>Posición</th>
                        <th>Fecha de Aplicación</th>
                        <th>Salario Esperado</th>
                        <th>Recomendado por</th>
                        <th>Estado</th>
                        <th>Acciones</th>
                    </tr>
                </thead>
                <tbody>
                    {{#each aplicaciones}}
                    <tr>
                        <td>{{this.nombre}}</td>
                        <td>{{this.fecha_aplicacion}}</td>
                        <td>{{this.salario_aspirante}}</td>
                        <td>{{this.recomendado_por}}</td>
                        <td>{{this.estado}}</td>
                        <td>
                            <form action="/auth/editarAplicacion/{{this.id}}" method="GET" style="display:inline;">
                                <button type="submit">Editar</button>
                            </form>
                            <form action="/auth/eliminarAplicacion" method="POST">
                                <input type="hidden" name="aplicacionId" value="{{this.id}}">
                                <button class="button button-small button-danger" type="submit">Eliminar</button>
                            </form>
                        </td>
                    </tr>
                    {{/each}}
                </tbody>
            </table>
                </div>
            </section>
        </main>
    </div>
</body>
</html>