<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Gestión de Trabajos - Sistema de Reclutamiento de HR</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <div class="container">
        <header class="header">
            <h1>Gestión de Trabajos</h1>
            <nav class="navigation">
                <ul>
                    <li><a href="/auth/admin" class="nav-link">Panel de Administración</a></li>
                    <li><a href="/login" class="nav-link button-secondary">Cerrar Sesión</a></li>
                </ul>
            </nav>
        </header>
        <main class="main-content">
            <section class="dashboard-section">
                <div class="button-container">
                    <a href="/agregar-trabajo" class="button button-primary">Agregar Trabajo</a>
                </div>
                <div class="crud-container">
                    <table class="crud-table">
                        <thead>
                            <tr>
                                <th>Nombre</th>
                                <th>Departamento</th>
                                <th>Nivel de Riesgo</th>
                                <th>Salario Mínimo</th>
                                <th>Salario Máximo</th>
                                <th>Estado</th>
                                <th>Acciones</th>
                            </tr>
                        </thead>
                        <tbody>
                            {{#each puestos}}
                            <tr>
                                <td>{{this.nombre}}</td>
                                <td>{{this.departamento}}</td>
                                <td>{{this.nivel_riesgo}}</td>
                                <td>{{this.salario_minimo}}</td>
                                <td>{{this.salario_maximo}}</td>
                                <td>{{this.estado}}</td>
                                <td>
                                    <form action="/auth/editar-trabajo/{{this.id}}" method="GET" class="inline-form">
                                        <button type="submit" class="button button-small button-primary">Editar</button>
                                    </form>
                                    <form action="/auth/eliminar-trabajo" method="POST" class="inline-form">
                                        <input type="hidden" name="id" value="{{this.id}}">
                                        <button type="submit" class="button button-small button-danger">Eliminar</button>
                                    </form>
                                </td>
                            </tr>
                            {{/each}}
                        </tbody>
                    </table>
                </div>
            </section>
        </main>
        <footer class="footer">
            <p>&copy; 2023 Sistema de Reclutamiento de HR. Todos los derechos reservados.</p>
        </footer>
    </div>
</body>
</html>