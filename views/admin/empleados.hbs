<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Gestión de Empleados - Sistema de Reclutamiento de HR</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <div class="container">
        <header class="header">
            <h1>Gestión de Empleados</h1>
            <nav class="navigation">
                <ul>
                    <li><a href="/admin" class="nav-link">Panel de Administración</a></li>
                    <li><a href="/logout" class="nav-link button-secondary">Cerrar Sesión</a></li>
                </ul>
            </nav>
        </header>
        <main class="main-content">
            <section class="dashboard-section">
                <div class="button-container">
                    <a href="/agregar-empleado" class="button button-primary">Agregar Empleado</a>
                </div>
                <div class="crud-container">
                    <table class="crud-table">
                        <thead>
                            <tr>
                                <th>Cédula</th>
                                <th>Nombre</th>
                                <th>Fecha Ingreso</th>
                                <th>Departamento</th>
                                <th>Puesto</th>
                                <th>Salario Mensual</th>
                                <th>Acciones</th>
                            </tr>
                        </thead>
                        <tbody>
                            {{#each empleados}}
                            <tr>
                                <td>{{this.cedula}}</td>
                                <td>{{this.nombre}}</td>
                                <td>{{this.fecha_ingreso}}</td>
                                <td>{{this.departamento}}</td>
                                <td>{{this.puesto}}</td>
                                <td>{{this.salario_mensual}}</td>
                                <td>
                                    <form action="/auth/editarEmpleado/{{this.id}}" method="GET" class="inline-form">
                                        <button type="submit" class="button button-small button-primary">Editar</button>
                                    </form>
                                    <form action="/auth/eliminarEmpleado" method="POST" class="inline-form">
                                        <input type="hidden" name="id" value="{{this.id}}">
                                        <button type="submit" class="button button-small button-danger">Eliminar</button>
                                    </form>
                                </td>
                            </tr>
                            {{/each}}
                        </tbody>
                    </table>
                </div>
            </section>
        </main>
        <footer class="footer">
            <p>&copy; 2023 Sistema de Reclutamiento de HR. Todos los derechos reservados.</p>
        </footer>
    </div>
</body>
</html>